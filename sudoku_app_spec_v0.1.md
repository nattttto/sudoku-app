# 数独アプリ 仕様書 v0.1

## 1. 概要

### 1.1 コンセプト

無料・広告なし・シンプルに遊べる数独アプリを作成する。

最大の特徴は、単に答えを提示する「ヒント」ではなく、

> **「なぜその数字になるのか」を数独の定石・論理に基づいて説明するヒント機能**

を重視すること。

AIによる推論・文章生成は使用せず、定型化された数独の解法ロジックをアプリ側に実装する。

---

## 2. 開発方針

### 2.1 基本方針

- 最初は簡素なUI・機能でMVPを完成させる
- ヒント機能・推論エンジンを最重要機能として設計する
- AIは使用せず、決定論的なルールベースでヒントを生成する
- 問題は原則としてアプリ側で自動生成する
- 将来的な機能追加を考慮し、問題生成・盤面管理・候補計算・ヒントエンジンを分離する
- 最初からFirebaseなどのバックエンドを必須にしない
- ローカルで完結する構成を基本とする

### 2.2 技術候補

- Next.js
- TypeScript
- React
- Vercel
- GitHub

※既存のVibe Coding環境を活用する。

---

# 3. MVP機能

## 3.1 必須機能

| 機能 | 内容 |
|---|---|
| 数独プレイ | 9×9の標準数独 |
| 問題生成 | アプリ側で自動生成 |
| 難易度 | Easy / Normal / Hard / Expert |
| 数字入力 | 1～9を入力 |
| 候補メモ | セルごとに候補数字を入力 |
| 候補計算 | 盤面から候補を計算 |
| ミスチェック | 行・列・ブロックの重複を検出 |
| ヒント | 次に実行可能な論理的手を提示 |
| ヒント解説 | 使用した定石と理由を説明 |
| ヒント可視化 | 関係するセル・候補を強調 |
| 正解判定 | 完成時にクリア判定 |
| タイマー | プレイ時間を表示 |
| Undo / Redo | 入力履歴を戻す・進める |
| リスタート | 問題を初期状態に戻す |

## 3.2 後回しにする機能

- ユーザーアカウント
- オンラインランキング
- オンライン対戦
- SNS連携
- 課金
- 広告
- AI機能
- クラウド保存
- 詳細なプレイ統計

---

# 4. 問題生成

## 4.1 基本方針

問題は原則として自動生成する。

基本的な生成フロー：

```text
完全な9×9盤面を生成
        ↓
数字をランダムに削除
        ↓
一意解かチェック
        ↓
ヒントエンジンで論理的に解けるか確認
        ↓
使用する解法から難易度判定
        ↓
問題として採用
```

## 4.2 問題の種類

将来的には以下の3種類を扱える設計にする。

### ① 自動生成問題

通常のプレイ用。

### ② 固定問題

あらかじめ用意した問題。

### ③ テスト用問題

特定の解法を検証するための開発者向け問題。

例：

```text
test-puzzles/
  naked-single.json
  hidden-single.json
  locked-candidate.json
  naked-pair.json
  x-wing.json
```

特定のヒントロジックが正しく動作することを確認するために使用する。

---

# 5. 数独盤面データ

## 5.1 基本データ

```ts
type Cell = {
  value: number | null
  candidates: number[]
  isGiven: boolean
  isError: boolean
}
```

### 各項目

- `value`
  - 現在入っている数字
  - 未入力の場合は `null`

- `candidates`
  - 現在の候補数字

- `isGiven`
  - 問題生成時から配置されている数字かどうか

- `isError`
  - ユーザー入力がルール違反の場合に使用

---

# 6. 候補計算エンジン

ヒント機能の基礎となる機能。

各セルについて、

- 同じ行
- 同じ列
- 同じ3×3ブロック

に存在する数字を確認し、入力可能な数字を候補として計算する。

例：

```text
候補 = {1,2,3,4,5,6,7,8,9}
        -
行に存在する数字
        -
列に存在する数字
        -
ブロックに存在する数字
```

結果として、

```text
候補 = {2,5,7}
```

などを得る。

---

# 7. ヒントエンジン

## 7.1 最重要機能

このアプリの中心機能。

現在の盤面から、

```text
現在の盤面
   ↓
候補を計算
   ↓
各種解法ルールを検索
   ↓
最初に発見した論理的な手を取得
   ↓
ヒントとして提示
```

という流れで処理する。

---

# 8. ヒントルール

## 8.1 初期実装

まず以下から実装する。

### ① Naked Single

あるセルに残っている候補が1つだけの場合。

例：

```text
R4C7 = {5}
```

説明：

> R4C7には5しか入れることができません。
> 同じ行・列・3×3ブロックにある数字を除外すると、候補が5だけになります。

---

### ② Hidden Single

ある行・列・3×3ブロックの中で、特定の数字を置けるセルが1つしかない場合。

例：

```text
この行で「5」を置ける場所
R2C3
R2C7
R2C9

ではなく、

R2C7だけ
```

説明：

> この行には5がまだありません。
> 5を置ける場所はR2C7しかないため、R2C7 = 5です。

---

### ③ Locked Candidates

3×3ブロック内で、ある数字の候補が同一行または同一列に限定されている場合、その行・列のブロック外から候補を削除する。

別名：

- Pointing
- Claiming
- Box-Line Reduction

---

### ④ Naked Pair

同じ行・列・ブロック内で、

```text
R2C3 = {2,7}
R2C8 = {2,7}
```

のように2セルが同じ2候補だけを持つ場合、その他のセルから2・7を削除する。

説明：

> R2C3とR2C8は、どちらも2または7しか入りません。
> この2セルで2と7を使用するため、同じ行の他のセルから2と7を除外できます。

---

# 9. 将来追加するヒントルール

実装優先度を分けて段階的に追加する。

## Lv.2

- Hidden Pair
- Naked Triple
- Hidden Triple

## Lv.3

- X-Wing
- XY-Wing

## Lv.4

- Swordfish
- その他の高度な数独テクニック

---

# 10. ヒントの優先順位

ヒントは簡単な解法から順番に検索する。

例：

```text
Naked Single
↓
Hidden Single
↓
Locked Candidates
↓
Naked Pair
↓
Hidden Pair
↓
Naked Triple
↓
Hidden Triple
↓
X-Wing
↓
XY-Wing
↓
Swordfish
```

最初に発見された有効な手を「次におすすめする手」として提示する。

ただし、将来的にはユーザーがヒントレベルを選択できるようにしてもよい。

---

# 11. ヒントデータ構造

各ヒントは、単なる答えではなく「推論情報」を返す。

```ts
type Hint = {
  technique: string
  targetCell?: string
  value?: number
  relatedCells: string[]
  explanation: string
  eliminations?: {
    cell: string
    value: number
  }[]
}
```

例：

```ts
{
  technique: "HIDDEN_SINGLE",
  targetCell: "R4C7",
  value: 5,
  relatedCells: [
    "R4C1",
    "R4C3",
    "R4C7",
    "R4C9"
  ],
  explanation: "この行では5を置ける場所がR4C7しかありません。",
  eliminations: []
}
```

---

# 12. ヒントUI

## 12.1 基本思想

ヒントを押した瞬間に答えを出さない。

「考える → 少し教える → 答えを教える」の段階式にする。

### STEP 1：考え方

```text
💡 ヒント

「5」に注目してみましょう。

この3×3ブロックの中で、
5を入れられる場所を探してみましょう。
```

### STEP 2：対象を提示

```text
もう少しヒント

5を入れられる場所は
この3×3ブロックでは1か所だけです。

→ R4C7
```

### STEP 3：答えと理論

```text
答え

R4C7 = 5

これは「Hidden Single」という
推論を使っています。
```

---

# 13. ヒントの可視化

今回のアプリの重要な差別化要素。

## 13.1 基本

- 対象セルを強調
- 関係するセルを強調
- 関係しないセルを薄く表示
- 候補数字を表示
- 消去される候補を明示

## 13.2 Naked Single

```text
候補
{5}
```

となったセルを強調。

## 13.3 Hidden Single

対象の行・列・ブロックを強調し、

```text
5を置ける候補
↓
R4C7
```

となる過程を視覚的に示す。

## 13.4 Naked Pair

```text
R2C3 → {2,7}
R2C8 → {2,7}
```

を強調。

その後、

```text
その他のセル
2・7を候補から削除
```

という変化を表示する。

## 13.5 X-Wing

4つの対象セルを線などで結び、

```text
R2C2 ───────── R2C8
  │              │
  │              │
R7C2 ───────── R7C8
```

のように、推論関係を視覚的に表現する。

---

# 14. 難易度判定

単純に「初期数字の数」だけでは判定しない。

ヒントエンジンで問題を解き、

```text
どの解法が必要だったか
```

を基準に難易度を決定する。

例：

| 難易度 | 主な必要テクニック |
|---|---|
| Easy | Naked Single / Hidden Single |
| Normal | Locked Candidates / Naked Pair |
| Hard | Hidden Pair / Triple系 |
| Expert | X-Wing / XY-Wingなど |

例：

```text
問題解析

Naked Single      ✓
Hidden Single     ✓
Locked Candidate  ✓
Naked Pair        ✓
X-Wing            ✕

→ Normal
```

※実際の難易度境界は、実装後にプレイ感を見ながら調整する。

---

# 15. 問題生成とヒントエンジンの連携

問題生成後に自動解析を行う。

```text
問題生成
   ↓
一意解チェック
   ↓
ヒントエンジンで解く
   ↓
使用したテクニックを記録
   ↓
難易度判定
   ↓
条件に合わなければ破棄
```

これにより、

> 「生成したけど、難易度が想定と違う」

という問題を減らす。

また、

> 「Expert問題なのにX-Wingすら必要ない」

などの状態も防げる。

---

# 16. SolverとHint Engineの役割分離

内部では、

### Solver

「この問題を解けるか・最終的な解答は何か」を担当。

### Hint Engine

「今の盤面から、どの論理テクニックを使えば次の一手を導けるか」を担当。

この2つは分離する。

```text
Sudoku Engine
├── Board Manager
├── Candidate Engine
├── Solver
├── Puzzle Generator
└── Hint Engine
    ├── Naked Single
    ├── Hidden Single
    ├── Locked Candidate
    ├── Naked Pair
    └── ...
```

---

# 17. 推奨ディレクトリ構成

```text
src/
├── app/
│   ├── page.tsx
│   └── sudoku/
│       └── page.tsx
│
├── components/
│   ├── SudokuBoard.tsx
│   ├── SudokuCell.tsx
│   ├── NumberPad.tsx
│   ├── CandidateGrid.tsx
│   ├── HintPanel.tsx
│   └── GameControls.tsx
│
├── features/
│   └── sudoku/
│       ├── board/
│       │   ├── types.ts
│       │   └── board.ts
│       │
│       ├── candidates/
│       │   └── candidateEngine.ts
│       │
│       ├── generator/
│       │   ├── generator.ts
│       │   └── difficulty.ts
│       │
│       ├── solver/
│       │   └── solver.ts
│       │
│       └── hints/
│           ├── types.ts
│           ├── hintEngine.ts
│           ├── nakedSingle.ts
│           ├── hiddenSingle.ts
│           ├── lockedCandidates.ts
│           ├── nakedPair.ts
│           ├── hiddenPair.ts
│           └── xWing.ts
│
└── data/
    └── test-puzzles/
```

---

# 18. 画面構成

## 18.1 Home

シンプルなスタート画面。

```text
Sudoku

[ EASY ]
[ NORMAL ]
[ HARD ]
[ EXPERT ]

────────────

[ 続きから ]
```

---

## 18.2 Play

```text
Sudoku

00:12:43

┌───────────────┐
│               │
│    9×9盤面    │
│               │
└───────────────┘

  1 2 3
  4 5 6
  7 8 9

[候補] [ヒント] [戻る]
```

MVPでは装飾を増やしすぎず、盤面を最優先する。

---

## 18.3 Hint

```text
💡 ヒント

Hidden Single

この3×3ブロックには
「5」がまだありません。

5を置ける場所を探してみましょう。

[もう少しヒント]

[答えを見る]
```

---

# 19. データ保存

MVPではサーバー側への保存は不要。

基本的にはブラウザ側の状態として管理する。

将来的に必要になった場合、

```text
Firebase
├── users
├── games
├── statistics
└── puzzle-history
```

などを追加する。

---

# 20. テスト方針

ヒントエンジンは重点的にテストする。

例えば、

```text
Naked Single
→ Naked Singleを正しく検出できる

Hidden Single
→ Hidden Singleを正しく検出できる

Naked Pair
→ 候補を正しく削除できる

X-Wing
→ X-Wingの条件を正しく検出できる
```

というユニットテストを用意する。

また、各テクニック専用の固定問題を用意し、

```text
盤面
↓
Hint Engine
↓
期待するTechnique
↓
期待するCell
↓
期待するCandidate elimination
```

を検証する。

---

# 21. MVPの開発順序

## Phase 1：盤面

- 9×9盤面
- 数字入力
- 数字削除
- 問題数字の固定
- エラー表示
- 候補メモ
- Undo / Redo

## Phase 2：数独エンジン

- 候補計算
- 正解判定
- Solver
- 一意解判定

## Phase 3：問題生成

- 完成盤面生成
- 数字削除
- 一意解チェック
- 難易度判定

## Phase 4：ヒントエンジン

1. Naked Single
2. Hidden Single
3. Locked Candidates
4. Naked Pair

## Phase 5：ヒントUI

- ヒントパネル
- 段階的ヒント
- 対象セル強調
- 関係セル強調
- 候補削除の可視化
- 理論名表示
- 理由説明

## Phase 6：高度な解法

- Hidden Pair
- Naked Triple
- Hidden Triple
- X-Wing
- XY-Wing
- Swordfish

---

# 22. アプリの最重要UX方針

### 通常の数独アプリ

```text
ヒント
 ↓
R4C7 = 5
```

### このアプリ

```text
ヒント
 ↓
「5」に注目
 ↓
関係する範囲を表示
 ↓
候補を表示
 ↓
なぜ他の場所ではないか説明
 ↓
使用した定石を表示
 ↓
R4C7 = 5
```

**「答え」ではなく「考え方」を提供することを最優先とする。**

---

# 23. 最終的なプロダクトコンセプト

> **「答えを教える数独」ではなく、「数独の解き方を教える数独」。**

広告・課金・余計な機能を極力排除し、シンプルに数独を楽しめることを基本とする。

そのうえで、

> 「この数字はなぜ入るの？」

という疑問に対して、

> 「この定石を使うと、ここまで候補を絞れるから」

と論理的・視覚的に説明できることを最大の特徴とする。

---

# 24. 将来的な拡張候補

MVP完成後に検討する。

- プレイ履歴
- 最速タイム
- 日別・月別統計
- デイリー数独
- 問題共有
- 問題IDによる再現
- ヒント使用回数の記録
- 使用した解法の統計
- 「Naked Pairを何回使ったか」などの学習記録
- 解法チュートリアル
- 解法ごとの練習モード
- PWA化
- オフラインプレイ
- スマートフォン向けUI最適化

特に将来的には、

> **「今日はNaked Pairを練習する」**

のような「数独学習アプリ」方向への拡張も可能。

---

# 25. MVP完成条件

以下を満たした時点でMVP完成とする。

- [ ] 9×9数独をプレイできる
- [ ] 問題を自動生成できる
- [ ] 生成問題が一意解を持つ
- [ ] 数字を入力・削除できる
- [ ] 候補を入力・確認できる
- [ ] Undo / Redoができる
- [ ] 正解判定ができる
- [ ] Naked Singleを検出できる
- [ ] Hidden Singleを検出できる
- [ ] Locked Candidatesを検出できる
- [ ] Naked Pairを検出できる
- [ ] ヒント対象セルを可視化できる
- [ ] ヒントの理由を文章で説明できる
- [ ] 使用した解法名を表示できる
- [ ] 「もう少しヒント」と「答えを見る」を段階的に提供できる
- [ ] AIなしでヒントが生成される
- [ ] PC・スマートフォンの両方で基本プレイできる

